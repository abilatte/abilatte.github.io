---
title: "LeetCode - 2559. 统计范围内的元音字符串数"
date: 2026-07-20T22:00:54+08:00
draft: false
categories: [Linux]
tags: [前缀和]
---

## 题目链接

>   [2559. 统计范围内的元音字符串数 - 力扣（LeetCode）](https://leetcode.cn/problems/count-vowel-strings-in-ranges/description/)



## 题目描述

>   给你一个下标从 **0** 开始的字符串数组 `words` 以及一个二维整数数组 `queries` 。
>
>   每个查询 `queries[i] = [li, ri]` 会要求我们统计在 `words` 中下标在 `li` 到 `ri` 范围内（**包含** 这两个值）并且以元音开头和结尾的字符串的数目。
>
>   返回一个整数数组，其中数组的第 `i` 个元素对应第 `i` 个查询的答案。
>
>   **注意：**元音字母是 `'a'`、`'e'`、`'i'`、`'o'` 和 `'u'` 。



## 思路

>   同  [2026-07-20-LeetCode---303.-区域和检索---数组不可变.md](2026-07-20-LeetCode---303.-区域和检索---数组不可变.md) 



## 题解

```cpp
class Solution {
public:
    vector<int> vowelStrings(vector<string>& words,
                             vector<vector<int>>& queries) {
        int n = words.size();
        vector<int> ans{};
        vector<int> pre_sum(n + 1);
        // 判断是否是元音字母
        auto isVowel = [](char c) -> bool {
            return c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u';
        };
        // 将元音开头结尾的字符串数目加入
        for (int i = 0; i < n; i++) {
            bool valid = isVowel(words[i].front()) && isVowel(words[i].back());
            pre_sum[i + 1] = pre_sum[i] + valid;
        }
        
        for (const auto& query : queries) {
            int left = query[0], right = query[1];
            ans.push_back(pre_sum[right + 1] - pre_sum[left]);
        }
        return ans;
    }
};
```
